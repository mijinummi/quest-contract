[package]
name = "prize_pool"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = "21.0.0"

[dev-dependencies]
# enable test utilities (register_contract helpers, mock env helpers)
soroban-sdk = { version = "21.0.0", features = ["testutils"] }
